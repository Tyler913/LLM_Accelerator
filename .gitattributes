# Normalize repository source text across platforms.
* text=auto eol=lf

# FPGA and model artifacts must never undergo line-ending conversion.
*.bit binary
*.xsa binary
*.elf binary
*.qtk binary
*.bin binary
*.pdf binary
